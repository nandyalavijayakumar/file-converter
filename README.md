# My File Converter (Next.js)
Simple file converter app (Text↔PDF, Word→PDF, Image→PDF, PDF→Images).

## Quick start
1. Install: `npm install`
2. Run dev: `npm run dev`
3. Open: http://localhost:3000

Note: This starter uses client-side libraries (pdf-lib, mammoth, pdfjs-dist).
